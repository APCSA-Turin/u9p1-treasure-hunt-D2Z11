package com.example.project;

//Dot only needs a constructor. Dot is used as an "empty" square.
public class Dot extends Sprite{
    // Setup x and y by using superclass (Sprite)
    public Dot(int x, int y) {
        super(x, y);
    }
}